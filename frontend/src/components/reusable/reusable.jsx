import React from 'react';
import reusablePNG from "./reusable.png";

const Reusable = () => (
  <img className="reusable reusable-task-show" alt="reusable" src={reusablePNG} />
)

export default Reusable;
